http:
  middlewares:
    swagger-path:
      replacePathRegex:
        regex: "^/(.*)"
        replacement: "/apiDocs/v1/$1"

    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true