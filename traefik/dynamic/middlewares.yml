http:
  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true

    swagger-path-rewrite:
      stripPrefix:
        prefixes:
          - "/"
      replacePath: "/apiDocs/v1"
